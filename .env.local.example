# スライド生成 API（Lambda + API Gateway）のエンドポイントURL
# 例: https://xxxxxxxx.execute-api.ap-northeast-1.amazonaws.com/production/generate-slides
NEXT_PUBLIC_LAMBDA_API_URL=

# Lambda で API_GATEWAY_KEY を設定している場合のみ。同じ値を設定する。
# 未設定の場合は空のままでよい。
NEXT_PUBLIC_API_KEY=
